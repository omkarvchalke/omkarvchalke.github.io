import { PagePlaceholder } from "@/components/page-placeholder";

export default function Page() {
  return (
    <PagePlaceholder
      eyebrow="Projects"
      title="Case studies, not cards — problem, architecture, tradeoffs, impact."
      phase="Phase 5"
    />
  );
}
