import type { Project } from "./types";

// Real case studies land here in Phase 5 (Phase 1 decision: 6+ projects,
// tiered `depth: "full" | "light"` — see Phase 1 §Decisions).
export const projects: Project[] = [];
